spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8010
          predicates:
            - Path=/auth/**
          filters:
            - AddResponseHeader=X-Powered-By, DanSON Gateway Service
