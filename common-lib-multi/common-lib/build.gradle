plugins {
    id 'java-library'
    id 'maven-publish'                // ✅ Add this line
    id 'org.springframework.boot' version '3.3.4' apply false
    id 'io.spring.dependency-management'
}

group = 'com.commonlib'
version = '1.0.0-SNAPSHOT'

dependencyManagement {
    imports {
        mavenBom "org.springframework.boot:spring-boot-dependencies:3.3.4"
    }
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    
    implementation 'io.jsonwebtoken:jjwt-api:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.11.5'
    
    // Add these for servlet + annotation classes used by filters
    compileOnly 'jakarta.servlet:jakarta.servlet-api:6.0.0'
    compileOnly 'jakarta.annotation:jakarta.annotation-api:2.1.1'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            groupId = project.group
            artifactId = 'common-lib'
            version = project.version
        }
    }
    repositories {
    mavenLocal()
    mavenCentral()
    
     //  // Publishes to your local Maven cache
       // maven {
            //name = "artifactory"
            //url = uri("https://artifactory.example.com/artifactory/libs-release-local") // placeholder
            //credentials {
               // username = findProperty("artifactory_user") ?: System.getenv("ARTIFACTORY_USER")
                //password = findProperty("artifactory_password") ?: System.getenv("ARTIFACTORY_PASSWORD")
            //}
       // }
    }
}
