server:
  port: 8013

rsa:
  public-key: classpath:certs/public.pem
  private-key: classpath:certs/private.pem
  
security:
  jwt:
    secret-key: 3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
    expiration-time: 3600000  # milliseconds (1 hour)

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/e-store
    username: root
    password: H123456s

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true

  sql:
    init:
      mode: always
      continue-on-error: true
logging:
  file:
    name: /Users/hasanain/Desktop/GitHub/Microservice/authentication-server/src/main/resources/app.log
  level:
    root: INFO                   # General log level
    org.springframework.security: DEBUG  
    org.springframework.web: DEBUG       # Optional: web logs
    org.hibernate.SQL: DEBUG             #
    org.hibernate.type.descriptor.sql: TRACE #